services:
  - type: worker
    name: telegram-bot
    env: python
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    runtime: python-3.11.9
